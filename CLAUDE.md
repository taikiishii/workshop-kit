# ワークショップ教材の共通ルール（AI 向け）

このファイルは workshop-kit を使うすべての教材リポジトリで共有しています。
各教材の `CLAUDE.md` が先頭でこれを読み込み、そのあとに教材固有のルールを足します。

## この教材の作りかた

- **編集してよいのは `content/` と `site.toml` だけ。**
  `docs/` は `python build.py` が作る生成物。直接編集しても次のビルドで消える。
- `docs/assets/` の共通部品（`deck.css` `deck.js` `index.css` `hakase*.png`）と
  `.gitignore` `.vscode/settings.json` は **workshop-kit が配る**。
  直したいときは workshop-kit 側（`wskit/assets/` `wskit/files/`）を直す。
  **その変更はすべての教材に効く**ので、片方だけの都合で変えないこと。
- 教材ごとに変えたいものは `site.toml` に足す。site.toml で足りないときは、
  kit 側に設定項目を作ってから使う。教材リポジトリに共通部品のコピーを作らない。
- 変更したら必ず `python build.py` を実行し、警告が出ていないことを確認する。

## 書くときのルール

- 読者は**小学校高学年〜中学生**。むずかしい言葉を使わず、漢字にはひらがなを添えない
  代わりに、やさしい言い回しを選ぶ。
- 記法の詳細は [AUTHORING.md](AUTHORING.md)。とくに次に注意：
  - **表（`| … | … |`）は使えない。** そのまま文字として出てしまう。箇条書きにする。
  - スライドの区切りは行頭の `---`。1枚に詰めこみすぎない（全幅で13行ぶんが目安）。
  - `> ` の吹き出しは博士のセリフ。`⚠` で注意色、`💪` でチャレンジ色になる。
  - 1枚に入る量は**行数を数えず、ブラウザで見て**判断する（AUTHORING.md「1枚に入る量」）。
- 事実（ピン番号・電流・仕様）は**必ず一次情報で裏を取る**。各教材の CLAUDE.md に
  一次情報のありかを書いてある。
- 既存の章の文体・見出しの付け方に合わせる。新しい書き方を持ちこまない。

## git

- **コミットメッセージは日本語**。何を変えたかを1行で書く（例: `発展編に e7 を追加`）。
- `docs/` は生成物だが GitHub Pages の配信元なので、**コミットに含める**。
  content を直したら build してから、content と docs を同じコミットに入れる。
- `archive/`（元の PowerPoint など）は `.gitignore` で除外済み。公開しない。
