# 資料の作り方（オーサリングガイド・共通編）

すべてのワークショップ教材に共通する書き方です。
**その教材だけのきまり**（セクション名・章フォルダ名の頭文字・使う言語・
ボード固有の注意）は、各教材リポジトリの `AUTHORING.md` にあります。両方に目を通してください。

教材は **Markdown だけ書けば作れます**。HTML・QRコード・もくじ（index）は
`python build.py` が自動生成するので、`docs/` の中を直接さわる必要はありません。

**1つの章 = 1つのフォルダ**です。フォルダの中に本文（`index.md`）と画像（`image/index/`）を入れます。
画像は VS Code で貼り付ければ自動で `image/index/` に保存されるので、フォルダは意識しなくてOKです。

```
site.toml                   ← この教材だけの設定（URL・セクション・色）
content/                    ← ここだけ編集する
  _template/index.md        ← 新しい章のひな形
  _index/                   ← もくじページに足す任意のブロック
    refs.html               ←   「📚 参考リンク」（なければ出ない）
    license.html            ←   第三者素材についての注記（なければ出ない）
  b1_intro/                 ← 章フォルダ（フォルダ名 = <id>_<slug>）
    index.md                ←   本文（Markdown）
    image/index/            ←   写真・画面キャプチャ（VS Codeが自動でここに保存）
assets/                     ← この教材だけの画像（ロゴなど）
build.py                    ← workshop-kit を呼ぶだけの数行
docs/                       ← 自動生成される公開ファイル（さわらない）
  b1_intro/{index.html, qr.svg, image/index/}   ← content と同じ形で出力される
  assets/{deck.css, deck.js, index.css, hakase*.png, site-theme.css, site-config.js, qr.svg}
  index.html
```

`docs/assets/` の中身と `.gitignore` `.vscode/settings.json` は
**workshop-kit が配る共通部品**です。手で直しても次のビルドで戻ります。
直したいときは workshop-kit 側を直してください（＝ぜんぶの教材に効きます）。

---

## 手順（4ステップ）

1. **新しい章を作る**
   ```bash
   python -m wskit new-chapter e7_newsensor --section 発展編 --title "⑦ あたらしいセンサー"
   ```
   （`cp -r content/_template content/e7_newsensor` と手でコピーしても同じです）
2. `content/e7_newsensor/index.md` を**Markdownで書く**（下の「書き方」参照）
3. 写真・画面キャプチャを本文に貼り付ける（VS Codeなら自動で `image/index/` に保存）
4. **ビルドして確認 → 公開**
   ```bash
   python build.py                 # docs/ を再生成（HTML・QR・もくじ）
   # docs/e7_newsensor/index.html をブラウザで開いて確認
   git add -A && git commit -m "発展編に e7 を追加" && git push
   ```
   push すると数十秒で GitHub Pages に反映されます。

> はじめて使うときだけ、しくみを入れます。
> ```bash
> git clone https://github.com/taikiishii/workshop-kit.git ../workshop-kit
> pip install -e ../workshop-kit
> ```

---

## 書き方

### フロントマター（ファイル先頭の設定）

| 項目 | 説明 | 例 |
|---|---|---|
| `id` | 章の番号。ならび順・QR・画像フォルダに使う | `b3` / `e4` |
| `slug` | URLになる英語名 | `pwm` |
| `section` | もくじのどのセクションに入れるか（**その教材の AUTHORING.md 参照**） | `基本編` |
| `emoji` | もくじカードの絵文字 | `🌗` |
| `num` | カードの番号バッジ | `3` / `④` |
| `color` | カードの枠色（`site.toml` の `[palette]` のキー） | `c1` |
| `nav_title` | 画面上部・タブに出るタイトル | `③ LEDの明るさを変えよう` |
| `card_title` | もくじカードの見出し | `LEDの明るさを変えよう` |
| `desc` | もくじカードの説明（1〜2文） | `ついている・消えているだけでなく…` |
| `level` | 難易度ラベル（`[palette.level]` を決めている教材だけ） | `入門編` |
| `wip` | もくじカードに「🚧 工事中」マークを付ける（作りかけの章） | `true` / `準備中` |

**書き上がったら `wip` の行を消す**と、もくじの「工事中」マークが外れます。

値のうしろに ` # …` と書いたメモは、ビルド時に取り除かれます。

### スライドの記法（本文）

| 書き方 | 意味 |
|---|---|
| `---`（行頭・単独） | スライドの区切り |
| `{cover}` | 表紙レイアウト（中央寄せ）。QRは自動で右下に付きます |
| `# 見出し` | スライドのタイトル |
| `:::` | ここで左右2カラムに分ける（左=説明、右=画像 または コード） |
| `1. ` / `- ` | 番号つき手順／箇条書き |
| `**太字**` `*斜体*` `` `コード` `` | 強調・コード表示 |
| `[リンク](url)` | リンク（別タブで開く） |
| `![キャプション](image/index/xxx.png)` | 画像（連続で書くと横並び） |
| `> ふつうの文` | 博士のヒント吹き出し（緑） |
| `> ⚠ 文` | 注意の吹き出し（オレンジ） |
| `> 💪 文` | チャレンジの吹き出し（青） |
| `>> 文` | 吹き出しを**全身の博士**に固定（見せ場用） |
| `>\| 文` | 吹き出しを**顔だけの博士**に固定 |
| ` ```<言語> ` 〜 ` ``` ` | コードブロック（色分け・行番号・コピーボタン付き） |
| ` ``` ` 〜 ` ``` `（言語なし） | ただの枠つきテキスト。**長いURLを配るとき**に便利（コピーボタンが付く） |
| `📷 説明` | **写真まち**の目印。点線の枠で「まだ写真がない」と示す（`> 📷 …` でも同じ） |

> 博士のアイコンは、本文が60文字未満なら**顔だけ**、60文字以上なら**全身**に
> 自動で切り替わります。意図的に変えたいときだけ `>>` ／ `>|` を使ってください。

> ⚠ **表（`| … | … |`）は使えません。** スライドではそのまま文字として出てしまいます。
> 比べたいときは「**項目 … Aはこう。Bはこう**」の形の箇条書きにしてください。

> **写真まちの目印は、わざと公開ページに残します。** オーナーがビルド成果物を見て
> 確認するので、まだ写真が入っていない場所がひと目で分かるようにするためです。
> 写真を貼ったら、その行を `![説明](image/index/xxx.png)` に置きかえてください。
> 全部そろったら frontmatter の `wip` を消します。

**定番の並び**は「表紙 →『なにをするの？』→ 手順①②… 」です。`content/_template/index.md` がその形になっています。

### スマホで見たときの表示

パソコンでは 16:9 のスライドですが、**スマホなど幅780px以下の画面では自動で縦長のカードに変わります**。
書き方は同じままで、次のように組み替わります。

- `:::` の左右2カラム → **上に説明・下に画像**の縦並び（画像は横幅いっぱいに大きく表示）
- 連続して書いた画像（横並び）→ こちらも**縦に1枚ずつ大きく**
- 文字サイズは端末で読めるサイズに調整されます

**発表モード**と**印刷 / PDF** はスマホでも 16:9 のスライドのままです
（スマホを縦に持ったまま発表モードにすると、画面を横向きに使うよう自動で回転します）。

---

### 「やってみよう」の書き方

**課題は箇条書きに、ぜんぶ入れる。吹き出しに課題を書かない。**

同じ重さの課題が「箇条書き」と「吹き出し」に分かれていると、
どれをやればいいのかが読み取れません。番号がついていれば
「3番まではやった」と数えられますし、メンターも進み具合を聞けます。

吹き出しに残してよいのは、**課題ではないもの**だけです。

- **章のまとめ・次の章への橋わたし** …「これで制御編は修了！」など
- **⚠ 注意** …「LEDをふやすときは1つずつに抵抗を」など
- **📷 写真まち**の目印

吹き出しが2つ以上つづくと博士が縦に並んで重くなるので、
`>|` で顔だけにするか、1つにまとめてください。

---

### 1枚に入る量

目安は **2段組の左カラムで10行ぶん・全幅で13行ぶん**（折り返し後）です。
本文は半角72文字/行、`> ` の吹き出しは半角60文字/行くらいで折り返します。

ただし **これはあくまで目安で、実際の判定はブラウザで見ること**。
行数を機械的に数えると、次の理由で**多めに出ます**。

- **吹き出しの行を二重に切り上げてしまう** … deck.js は連続する `> ` 行を
  `buf.join(" ")` で**1つの段落にまとめる**ので、原稿が2行でも表示は1本の文。
  原稿の行ごとに切り上げて足すと、1つの吹き出しで1〜2行ぶん多く出る。
- **`**` や `` ` `` を幅に数えてしまう** … 表示では消える文字が幅に入る。
- **「1行」の高さがそろっていない** … 本文の1行と吹き出しの中の1行は高さが違う。
  とくに全身の博士（`icon-full`）の吹き出しは、文字が2行でも本文4〜5行ぶんの高さ。

実例：UIAPduino の `b5_buzzer`「なぜ 7番ピン？」は数えると14行になりましたが、
実際の画面は下半分が空いていました。**数字ではなく画面を見て判断してください。**

> はみ出しそうなときは、文を削る前に **`>|` で吹き出しを顔アイコンにする**、
> **図の縦横比を横長にする**でもだいぶ変わります。

---

## コードの見せ方

プログラムは**コードブロック**で書きます。使う言語は教材ごとに違うので、
その教材の `AUTHORING.md` を見てください（`cpp` / `js` / `py` に対応しています）。

````markdown
```cpp はじめてのLチカ          ← 「言語」＋（省略可）「キャプション」
const int LED = 2;

void setup() {
  pinMode(LED, OUTPUT);
}
```
````

- 言語のうしろに書いた文字は、コードの上に**小見出し**として出ます
- 色分け・**行番号**は自動です
- その教材でよく出てくる関数・グループ名は**青く**表示されます
  （どの語を青くするかは `site.toml` の `[code] namespaces`）

表示は4つのモードで自動的に切りかわります。

| モード | 見え方 |
|---|---|
| **Web（パソコン）** | 右上の「📋 コピー」で**ワンクリックコピー**（行番号は入りません）。押すと「✅ コピーしました」に変わります |
| **スマホ** | 文字を読めるサイズに拡大。長い行は**折り返して**表示。コピーボタンは指で押せる大きさになります |
| **発表モード** | 行数に合わせて文字が自動で大きくなります。コピーボタンは**消えます**（クリックで次のスライドに進むため） |
| **印刷 / PDF** | 背景と色分けはそのまま印刷。コピーボタンは**消えます** |

書くときのコツ：

- **1スライドのコードは 14行くらいまで**。長いと自動で文字が小さくなります（分けるのがおすすめ）
- 字下げの幅は、その教材の開発環境が自動整形する幅に合わせる（教材ごとの AUTHORING.md 参照）
- `:::` の右カラムをコードだけにすると、コード側が少し広く表示されます

---

## 写真・画面キャプチャの渡し方

- いちばん簡単なのは **VS Code で画像をコピー → 本文に貼り付け**。自動で **`image/index/`** に保存され、
  `![](image/index/…)` の参照も入ります。**パスやフォルダは気にしなくてOK**
  （`.vscode/settings.json` で保存先を設定済み）
- 手で置くときも、その章の **`image/index/`** に入れて `![説明](image/index/step1.png)` と書けばOK
- ビルド時に `docs/<章>/image/index/` へコピーされ、大きすぎる画像は自動で縮小されます
- 名前は順番がわかるように `01_open.png` `02_wiring.png` … と付けると迷いません
- 形式は画面キャプチャなら **PNG** がおすすめ
- **配線・実物の写真**は、色と位置がはっきり分かる角度で撮る。真上から撮るのが基本

---

## マーキング（矢印・囲み・「ここをクリック」）

画像に矢印や囲みを付けたいときは、**Greenshot**（Windows・無料）が手軽でおすすめです。

### Greenshot のセットアップ

1. https://getgreenshot.org/ からダウンロードしてインストール
2. `PrintScreen` キーで範囲を選んでキャプチャ → そのまま注釈エディタが開く
3. ツールバーで **矢印・四角・ハイライト・番号・文字** を追加
4. 「名前を付けて保存」で `content/<章>/image/index/` に PNG 保存（または VS Code に貼り付け）

> **ShareX**（https://getsharex.com/ ）も同様に無料で高機能です。お好みで。

### マーキングのコツ（子ども向け）

- 押す場所・挿す穴は **赤い四角で囲む** ＋ 短い言葉（「①ここをクリック」「①ここに挿す」）
- 1枚に矢印を入れすぎない（多くても2〜3個）
- そもそも本文の「**番号つき手順**」＋写真で大半は伝わるので、マーキングは"ここぞ"だけに

---

## もくじのセクションを増やす・変える

`section` に書いた名前でカードが自動的に振り分けられます。
セクションの定義（名前・見出し・説明・区切り線の色）は、その教材の **`site.toml` の `[[sections]]`** です。
書いた順に表示され、セクションの中は `id` の昇順です。

```toml
[[sections]]
name  = "発展編"          # frontmatter の section: と一致させる
cls   = "s-adv"           # CSS クラス（重複しない名前）
title = "🚀 発展編"
sub   = "センサーで感じて、モーターで動かす"
line  = ["#c9b3e8", "#ffcf5c", "#ff8a80"]   # 区切り線のグラデーション
```

カードの枠色は `[palette]`、難易度ラベルの色は `[palette.level]` です。
`site.toml` に無い `section` や `color` を frontmatter に書くと、ビルド時に警告が出ます。

---

## 新しいワークショップを立ちあげる

```bash
python -m wskit new-workshop ../rasppi --name "ラズパイ 体験ワークショップ"
cd ../rasppi
# site.toml の base_url・セクション・色を直してから
python build.py
```

`site.toml` を書きかえるだけで、見た目と操作感は他の教材とそろいます。
